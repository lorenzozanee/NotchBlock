# v0.3.0: SwiftData Migration Plan

## Decision
Research confirmed: local-only SwiftData works with ad-hoc signing. WidgetKit deferred (needs $99/yr for App Groups).

## Changes
1. TimeBlock struct → @Model class (value→reference, let→var)
2. TimeBlockStore retires → ModelContainer + @Query
3. One-time UserDefaults JSON → SwiftData migration
4. All views update: @ObservedObject → @Query

## Files: 8-10 files modified
Models/TimeBlock.swift · Managers/TimeBlockStore.swift · NotchBlockApp.swift · all Views · BlockScheduler · StatisticsStore

## Risk: Medium — reference semantics change, keep old UserDefaults as backup
