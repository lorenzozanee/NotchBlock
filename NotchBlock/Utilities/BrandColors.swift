import SwiftUI

/// Unified brand color palette — indigo family.
/// Used for accent color, status indicators, and semantic coloring.
enum BrandColors {
    static let accent = Color(red: 79/255, green: 70/255, blue: 229/255)
    static let pending = Color.blue
    static let active = Color.green
    static let completed = Color.secondary
    static let missed = Color.red
}
