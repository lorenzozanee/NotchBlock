# NotchPanel V2 — 框架设计

## 核心规则

### 互斥显示 (Mutual Exclusion)
- 主排程窗口打开 → 刘海 hover 不触发 panel
- 主排程窗口关闭/隐藏 → 刘海 hover 正常触发 panel
- 打开主窗口时 → 自动关闭 notch panel

### Panel 生命周期
- 触发: 鼠标在刘海区 ≥ 0.5s + 主窗口未打开
- 保持: 鼠标在 panel 内
- 消失: 鼠标离开 panel ≥ 0.5s → 渐隐

## UI 布局 (320 x auto)

```
┌──────────────────────────────────────┐
│  ● 当前任务              ⏱ 23:45    │  ← active task + countdown
│  深度工作                            │
│  🕐 09:00 — 11:00        [✓ 完成]   │  ← time + quick complete
├──────────────────────────────────────┤
│  接下来                              │
│  ▎ 代码审查      10:00 · 1h    ✎    │  ← click to edit
│  ▎ 午休          12:00 · 1h    ✎    │
├──────────────────────────────────────┤
│  + 快速添加任务...                   │  ← inline add
└──────────────────────────────────────┘
```

## 交互
- hover 刘海 → panel 滑入 (0.35s)
- 鼠标移入 panel → 保持
- ✓ 完成 → 标记 completed + 震动反馈
- ✎ 编辑 → inline 编辑标题和时间
- + 添加 → 展开输入框
- 鼠标离开 panel → 0.5s 后渐隐

## 视觉
- .ultraThinMaterial 毛玻璃
- 圆角 18px
- 微妙边框渐变
- 倒计时 .contentTransition(.numericText())

## 涉及文件
| 文件 | 改动 |
|------|------|
| NotchPanelView.swift | 完全重写 |
| NotchPanelController.swift | 动画 + 互斥 |
| NotchTracker.swift | 主窗口可见性检查 |
| NotchBlockApp.swift | 暴露窗口状态 |

## 需要确认
1. 编辑是 inline 展开还是弹出 sheet？ 弹出sheet
2. 快速添加后是否自动关闭 panel？   关闭
3. panel 最大显示几个 upcoming 任务？ 5个且可滑动
